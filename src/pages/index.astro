---
// src/pages/index.astro - PART 1
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elvis Moreira</title>
    <meta name="description" content="Senior Full Stack Developer with 12+ years experience in Ruby, Node.js, React Native. Based in South Florida.">
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    
    <style>
        * {
            font-family: 'Inter', sans-serif;
        }
        
        .hero-gradient {
            background: linear-gradient(135deg, #5a4a7c 0%, #e85a5a 35%, #2c3e5c 100%);
            position: relative;
            overflow: hidden;
        }
        
        .hero-gradient::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, rgba(0,0,0,0.2) 0%, rgba(0,0,0,0.05) 50%, rgba(0,0,0,0.2) 100%);
            z-index: 1;
        }
        
        .accent-gradient {
					background: linear-gradient(135deg, #2c3e5c 0%, #1a2332 100%);
        }
        
        .glass-effect {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        }
        
        .highlight-text {
            background: linear-gradient(135deg, #2c3e5c 0%, #1a2332 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        /* Floating geometric shapes */
        .floating-shape {
            position: absolute;
            opacity: 0.1;
            animation: float 20s infinite ease-in-out;
        }
        
        .floating-shape:nth-child(1) {
            top: 20%;
            left: 10%;
            animation-delay: 0s;
        }
        
        .floating-shape:nth-child(2) {
            top: 60%;
            right: 10%;
            animation-delay: -7s;
        }
        
        .floating-shape:nth-child(3) {
            bottom: 20%;
            left: 20%;
            animation-delay: -14s;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            25% { transform: translateY(-20px) rotate(90deg); }
            50% { transform: translateY(-40px) rotate(180deg); }
            75% { transform: translateY(-20px) rotate(270deg); }
        }
        
        /* Responsive title sizing with better hierarchy */
        .hero-name {
            font-size: clamp(1.5rem, 4vw, 2.5rem);
            font-weight: 700;
            letter-spacing: 0.05em;
        }
        
        .hero-title {
            font-size: clamp(2.5rem, 8vw, 4rem);
            line-height: 0.9;
            font-weight: 900;
            letter-spacing: -0.02em;
        }
        
        .hero-subtitle {
            font-size: clamp(3rem, 10vw, 5rem);
            line-height: 0.8;
            font-weight: 900;
            letter-spacing: -0.03em;
        }
        
        @media (min-width: 768px) {
            .hero-name {
                font-size: clamp(2rem, 5vw, 3rem);
            }
            .hero-title {
                font-size: clamp(3.5rem, 8vw, 5.5rem);
            }
            .hero-subtitle {
                font-size: clamp(4rem, 10vw, 7rem);
            }
        }
        
        /* Enhanced button styles */
        .btn-primary {
            background: linear-gradient(135deg, #2c3e5c 0%, #1a2332 100%);
            border: none;
            box-shadow: 0 10px 20px rgba(232, 90, 90, 0.3);
        }
        
        .btn-primary:hover {
            box-shadow: 0 15px 30px rgba(232, 90, 90, 0.4);
            transform: translateY(-2px);
        }
        
        .btn-social {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 2px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        .btn-social:hover {
            background: rgba(255, 255, 255, 0.2);
            border-color: rgba(255, 255, 255, 0.4);
            transform: translateY(-2px) scale(1.05);
        }
        
        /* Tech stack improvements */
        .tech-pill {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
        }
        
        .tech-pill:hover {
            background: rgba(255, 255, 255, 0.25);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }
        
        /* Button responsiveness */
        .btn-group {
            gap: 1rem;
        }
        
        @media (max-width: 640px) {
            .btn-group {
                gap: 0.75rem;
            }
            
            .btn-group a {
                flex-shrink: 0;
            }
        }
        
        /* Content spacing improvements */
        .content-section {
            margin-bottom: clamp(2rem, 5vw, 3rem);
        }
        
                 /* Font Awesome fallback */
         .fas, .fab {
             font-family: 'Font Awesome 6 Free', 'Font Awesome 6 Brands', sans-serif;
             font-weight: 900;
         }
         
         .fab {
             font-weight: 400;
         }
         
         /* Fade in animation */
         .fade-in {
             opacity: 0;
             transform: translateY(30px);
             transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
         }
         
         .fade-in.loaded {
             opacity: 1;
             transform: translateY(0);
         }
    </style>
</head>

<body class="text-white overflow-x-hidden">
    <!-- Hero Section with Gradient Background -->
    <section class="min-h-screen hero-gradient relative">
        <!-- Floating Geometric Shapes -->
        <div class="floating-shape w-32 h-32 bg-white rounded-2xl transform rotate-45"></div>
        <div class="floating-shape w-24 h-24 bg-white rounded-full"></div>
        <div class="floating-shape w-40 h-20 bg-white rounded-2xl"></div>
        
        <div class="max-w-7xl mx-auto px-6 sm:px-8 lg:px-12 relative z-10">
            <div class="min-h-screen flex items-center">
                <div class="w-full">
                    <div class="fade-in">
                                                 <!-- Name -->
                         <div class="content-section">
                             <div class="hero-name mb-4 tracking-wider" style="text-transform: uppercase;">
                                 Elvis Moreira
                             </div>
                         </div>
                         
                         <!-- Main Title with Better Hierarchy -->
                         <div class="content-section">
                             <h1 class="mb-8" style="text-transform: uppercase;">
                                 <div class="hero-title text-white mb-2">Senior</div>
                                 <div class="hero-subtitle highlight-text mb-2">Full Stack</div>
                                 <div class="hero-title text-white">Developer</div>
                             </h1>
                         </div>
                        
                        <!-- Tech Stack Pills - Enhanced -->
                        <div class="content-section">
                            <div class="flex flex-wrap gap-3 mb-8">
                                <span class="tech-pill px-4 py-2 rounded-xl text-sm font-semibold text-white">Ruby on Rails</span>
                                <span class="tech-pill px-4 py-2 rounded-xl text-sm font-semibold text-white">Node.js</span>
                                <span class="tech-pill px-4 py-2 rounded-xl text-sm font-semibold text-white">React.js + Native</span>
                                <span class="tech-pill px-4 py-2 rounded-xl text-sm font-semibold text-white">Vue.js</span>
                                <span class="tech-pill px-4 py-2 rounded-xl text-sm font-semibold text-white">AWS</span>
                            </div>
                        </div>
                        
                        <!-- Description with Better Typography -->
                        <div class="content-section">
                            <div class="max-w-3xl">
                                <p class="text-lg lg:text-xl font-light leading-relaxed text-white/90">
                                    Seasoned full-stack developer with over <span class="font-semibold text-white">12 years of experience</span> building projects 
                                    <span class="font-semibold text-white">from scratch to production</span>. Creating 
                                    <span class="font-semibold text-white">enterprise-scale applications</span> and 
                                    <span class="font-semibold text-white">scalable solutions</span> that serve 
                                    <span class="font-semibold text-white">thousands daily</span>.
                                </p>
                            </div>
                        </div>
                        
                        <!-- Enhanced Buttons -->
                        <div class="content-section">
                            <div class="flex btn-group items-center">
                                <!-- Resume Button - Primary -->
                                <a href="/elvis-moreira_resume.pdf" target="_blank" class="btn-primary text-white px-8 py-4 rounded-xl font-bold text-lg transition-all duration-300 flex items-center group">
                                    <span class="mr-3">Resume</span>
                                    <i class="fas fa-download group-hover:translate-y-1 transition-transform duration-300"></i>
                                </a>
                                
                                <!-- Social Buttons - Secondary -->
                                <a href="mailto:contact@elvisnm.com" class="btn-social text-white px-4 py-4 rounded-xl font-bold text-lg flex items-center justify-center group">
                                    <i class="fas fa-envelope group-hover:rotate-12 transition-transform duration-300"></i>
                                </a>
                                
                                <a href="https://linkedin.com/in/elvisnm" target="_blank" class="btn-social text-white px-4 py-4 rounded-xl font-bold text-lg flex items-center justify-center group">
                                    <i class="fab fa-linkedin group-hover:rotate-12 transition-transform duration-300"></i>
                                </a>
                                
                                <a href="https://github.com/elvisnm" target="_blank" class="btn-social text-white px-4 py-4 rounded-xl font-bold text-lg flex items-center justify-center group">
                                    <i class="fab fa-github group-hover:rotate-12 transition-transform duration-300"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script>
                 // Enhanced fade-in animation
         window.addEventListener('load', function() {
             setTimeout(() => {
                 const fadeElements = document.querySelectorAll('.fade-in');
                 fadeElements.forEach((element, index) => {
                     setTimeout(() => {
                         element.classList.add('loaded');
                     }, index * 200);
                 });
             }, 300);
         });

         // Parallax effect for floating shapes
         window.addEventListener('scroll', function() {
             const scrolled = window.pageYOffset;
             const shapes = document.querySelectorAll('.floating-shape');
             
             shapes.forEach((shape, index) => {
                 const speed = 0.5 + (index * 0.2);
                 (shape as HTMLElement).style.transform = `translateY(${scrolled * speed}px) rotate(${scrolled * 0.1}deg)`;
             });
         });

         // Add stagger animation to tech pills
         window.addEventListener('load', function() {
             setTimeout(() => {
                 const techPills = document.querySelectorAll('.tech-pill');
                 techPills.forEach((pill, index) => {
                     setTimeout(() => {
                         (pill as HTMLElement).style.opacity = '0';
                         (pill as HTMLElement).style.transform = 'translateY(20px)';
                         (pill as HTMLElement).style.transition = 'all 0.5s ease';
                         
                         setTimeout(() => {
                             (pill as HTMLElement).style.opacity = '1';
                             (pill as HTMLElement).style.transform = 'translateY(0)';
                         }, 50);
                     }, 1000 + (index * 100));
                 });
             }, 100);
         });
    </script>
</body>
</html>